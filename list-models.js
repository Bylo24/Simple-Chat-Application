require('dotenv').config({ path: '.env.local' }); const axios = require('axios'); async function listModels() { try { const GEMINI_API_KEY = process.env.EXPO_PUBLIC_GEMINIKEY; console.log('API Key defined:', !!GEMINI_API_KEY); const LIST_MODELS_URL = 'https://generativelanguage.googleapis.com/v1/models'; console.log('Using URL:', LIST_MODELS_URL); const response = await axios.get(LIST_MODELS_URL + '?key=' + GEMINI_API_KEY); console.log('Available Models:', JSON.stringify(response.data, null, 2)); } catch (error) { console.error('Error:', error.message); if (error.response) { console.error('Status:', error.response.status); console.error('Data:', JSON.stringify(error.response.data, null, 2)); } } } listModels();
