require('dotenv').config({ path: '.env.local' }); const axios = require('axios'); async function testGeminiApi() { try { const GEMINI_API_KEY = process.env.EXPO_PUBLIC_GEMINIKEY; console.log('API Key defined:', !!GEMINI_API_KEY); const GEMINI_API_URL = 'https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent'; console.log('Using URL:', GEMINI_API_URL); const response = await axios.post(GEMINI_API_URL + '?key=' + GEMINI_API_KEY, { contents: [{ parts: [{ text: 'Hello, can you give me a short test response?' }] }], generationConfig: { temperature: 0.2, maxOutputTokens: 100 } }); console.log('API Response:', JSON.stringify(response.data, null, 2)); } catch (error) { console.error('Error:', error.message); if (error.response) { console.error('Status:', error.response.status); console.error('Data:', JSON.stringify(error.response.data, null, 2)); } } } testGeminiApi();
